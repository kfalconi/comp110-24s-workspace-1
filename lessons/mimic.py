def mimic(my_words: str, letter_idx: int)
    """Outputs the characters of my_index at letter_idx """
    if letter_idx >= len(my_words):
        "Index is too high"
    else:
        return my_words[letter_idx]

